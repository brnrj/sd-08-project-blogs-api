const userValidations = require('./user');
const loginValidatetions = require('./login');
const categoriesValidations = require('./categories');
const postValidations = require('./posts');

module.exports = {
  userValidations,
  loginValidatetions,
  categoriesValidations,
  postValidations,
};
