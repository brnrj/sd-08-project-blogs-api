const userControler = require('./users');
const loginControler = require('./login');
const categoriesControler = require('./categories');

module.exports = {
  userControler,
  loginControler,
  categoriesControler,
};
